#include <iostream>
#include <iomanip>
#include <string>
#include <cstring>
#include <fstream>
using namespace std;

int main()
{

    cout << (5 << 2) << endl; // Left shift operator << (multiply of 2 with that times)
    cout << (6 << 4) << endl;

    cout << (6 >> 1) << endl; // Right shift operator >> 
    cout << (20 >> 2) << endl;
    return 0;
}